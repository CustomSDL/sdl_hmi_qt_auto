formatVersion: 1
formatType: am-application
---
id: 'com.test.pelux_browser'
icon: 'icon.png'
runtime: 'native'
code: 'quicknanobrowser'
name:
    en: 'quicknanobrowser'

# As applications are run as root - browser complains, so we workaround. We are sorry.
runtimeParameters:
    arguments:
        - "--no-sandbox"
