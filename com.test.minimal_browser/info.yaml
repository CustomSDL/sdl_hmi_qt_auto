formatVersion: 1
formatType: am-application
---
id: 'com.test.browser_app'
icon: 'icon.png'
runtime: 'native'
code: 'minimal.pro'
name:
    en: 'minimal_app'

# As applications are run as root - browser complains, so we workaround. We are sorry.
runtimeParameters:
    arguments:
        - "--no-sandbox"
